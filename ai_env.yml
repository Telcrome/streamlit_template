name: streamlit_template_minimal_env
dependencies:
  - python=3.8
  - anaconda
  - pip
  - pytorch::pytorch
  - pytorch::torchvision
  - cudatoolkit=10.2
  - conda-forge::python-graphviz
  - pip:
    - pylama # Used for vscode linting
    - opencv-python
    - pydicom
    - psycopg2-binary
    - flexx
    - gym # Optional, only used for reinforcement learning benchmarks
